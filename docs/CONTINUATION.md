# Latest — wallet, Arkiv publication and reviewer enrollment (12 September)

**Live source `55bef1b`, deployment `dpl_D58ymL6WPhnUvc3otDUeAvbkmpba`**, https://cutout-ethrome-2026.vercel.app. Same public contracts and issuer setup; the deployment rebuilds WASM with browser holder generation. These observations supersede older "nextJob 0", Swarm-session and CLI-only holder prerequisites below.

- Connected header now displays wallet/Fuji and resets when the session changes. Arkiv publication handles unknown-chain4902 with approved SDK metadata, verifies network/account after switching, reports sanitized actionable wallet errors, and checks test GLM. Default lease900blocks (~30min); acceptance deadline remains independent.
- Actual funded public task **#1** exists, 1testUSDC, creator `0x746bb7beFD31D9052BB8EbA7D5dD74C9aCf54C6d`. At17:08UTC it was open with no matching active Arkiv listing. Creator had zero Tiramisu gas. We sent0.01testGLM from the authorized operator client; receipt confirmed. Creator must sign publication from their own wallet; no signing access to that wallet exists here. Do not fund a duplicate task. Recheck deadline/state before retry.
- Reviewer → Wallet & storage readiness → Set up qualification opens real browser enrollment. Generate/save private holder backup, share only the separate enrollment request privately. Demo issuer approval and private credential delivery remain operator-assisted. No automatic expertise assessment or professional accreditation. The onchain proof verifies issuer signature, expiry, nonrevocation and task/wallet binding.
- Operator helper `node --import tsx experiments/qualification/pilot/issue-enrollment.mjs --request <public request.json> --out <new private credential.json>` checks finalized public issuer state and uses the existing persistent private registry. It never requests holder secrets or creates a server signing endpoint. A fresh root-owned browser test holder/request/credential exists under ignored0600 `.runtime/cutout-enrollment/`; preserve it and the allocation history. It was issued with the existing Fuji issuer, without a root update, and expires1789320633. It belongs to the operator's test, not the teammate. Do not send its private backup to the teammate.
- Verification:45focused tests, TS/build; actual production browser enrollment/downloads/clear/390px, existing guided walkthrough, disposable-issuer holder/proving regression; new operational credential proof accepted by actual Fuji verifier and altered recipient rejected via read-only calls. No public accept/deliver/pay lifecycle claimed. [Durable evidence](design/cutout/evidence/enrollment-publication/README.md).

Next: teammate refreshes and retries publication of the existing still-eligible task, then prepares their own enrollment request. Sign only the approved public request, return the credential privately, and complete the real two-role manual test. Public paid lifecycle and real Arkiv native-expiry evidence remain outstanding. Do not ask for Swarm setup or private holder/wallet keys. Completed isolated worker `credential_enrollment_browser` commit9e437e6 integrated as c6607d1; wallet helper2c4d03b integrated as9adf0e8. No unfinished worker ownership.

---

# Current handoff — Cutout deployed (12 September 2026)

User-facing tutorials: `docs/CUTOUT-JUDGE-TUTORIAL.md` is the no-setup walkthrough; `docs/CUTOUT-MANUAL-TEST.md` now maps each action to its funding/credential needs and gives the real two-profile flow with expected results. Documentation only; no new public lifecycle was executed by writing these guides.

**Public Cutout:** https://cutout-ethrome-2026.vercel.app (guided demo: `?view=demo`). Actual Vercel deployment `dpl_BaPyTpRQXJupsZy7wTrKXTQbadv4`, clean application source `6e923a6`. Both the original Review Pass URL and new Cutout alias serve this deployment; do not redirect the original origin because existing rehearsal browser keys/sessions belong to it. The Cutout hostname is now a verified project production domain, so future production deploys should assign it automatically. A bare manual alias initially inherited Vercel SSO; registering the actual project domain fixed public access without disabling preview protection. Verify the hostname unauthenticated after every promotion. Do not relink Git integration or deploy root EXIT config.

**Latest storage correction:** user requires no customer Swarm setup. Active hosted build now deliberately selects `swarm-gateway`; direct browser uploads use gateway-funded postage and independently hash-verify retrieval before success. No Swarm ID account/iframe/drive required. `createGatewayStorage` preserves public refs on retrieval failure and retries reads only; existing recipient crypto and wallet checks unchanged. Actual clean-browser public ciphertext roundtrip/both recipients/reload/outsider and production desktop/mobile checks pass. See `docs/design/cutout/evidence/account-free-storage`. Public gateway is a trial service with unspecified retention; UI and README disclose temporary demo storage. Team gift drive/keys untouched. Swarm ID is optional under supplied bounty requirements.

**Next executable work:** run the funded public lifecycle using the existing operator profiles/keys and valid class-7 credential, then public Arkiv creation/native-expiry proof. Do not ask for Swarm login, additional gift codes or funds; previous authentication blocker below is superseded. This storage pass performed no funding, acceptance, payment, key rotation or issuer writes. Existing reviewer browser had no app tab, but its old-origin profile must still be preserved.

Actual production Chromium verification passed desktop/mobile role navigation, full guided AES-GCM walkthrough, drag/keyboard exact decryption and offline-configuration fallback with zero page exceptions/storage writes. Evidence: `docs/design/cutout/evidence/hosted` and `cutout-domain`; build hashes and local lifecycle evidence alongside. Both original funded browser profiles retain their old-origin registered keys. The account sessions were not accessed or copied in this design pass.

The user renamed Review Pass to **Cutout** and explicitly prioritized customer-facing design using six supplied HTML references. Read `docs/design/cutout/DIRECTION.md`; references are preserved alongside it. Root branch remains `review-pass/product`, historical requirements and provenance preserved. Never rename protocol domains or browser key namespaces merely for branding.

Latest technical follow-up: user assigned teammate the manual review and requested engineering finish independent work. `docs/CUTOUT-MANUAL-TEST.md` is the concise human checklist. Arkiv now retries transient query/hydration failure on genuine heads, restarts failed initial/established subscriptions with a bounded retry budget, gates live state on both actual subscription ACKs, and lets manual Refresh restart exhausted streams. Client listing receipts and outcomes survive wallet network changes. Agent commits 6844e33/2689fde integrated as d4fc92c/dfa6054, root changes 90a8bfa/afe105f. 16 listing tests and 26 journey/settlement/storage tests pass, 1 optional external browser case skipped; TS/build pass. Isolated actual-browser/public-WSS fault probes recover initial failure and established closure; no writes/expiry claimed. See `docs/design/cutout/evidence/release-followup` for current deployment/evidence.

Actual read-only preflight at 16:01 UTC: rehearsal client has 20 test USDC, 0.1 Tiramisu gas token and ~3.05 Fuji AVAX; reviewer has ~0.05 Fuji AVAX; nextJob is 0; snapshot returns HTTP 200. Swarm upload sessions still absent in isolated role browsers (client app reports disconnected; reviewer had no matching app tab). No secret/session extraction or signer reuse attempted. This is the remaining access block for funded public scope/report upload and subsequent expiry/payment tests; the teammate should not be asked to debug infrastructure or fund wallets again. Existing old-hostname browser profiles/keys preserved.

New shell: Tasks / Activity / Your workspace, client/reviewer roles, live brief/reward preview, integrated Swarm account control, and actual retrieval/decryption via cut-open interaction. Approval now requires opening the committed report in the current browser view. Full browser-only guided demo is explicitly simulated for qualification/payment and uses actual WebCrypto AES-GCM for report sealing/opening. This is not the Fuji bounty lifecycle.

Verification so far: isolated fresh local deployment `.runtime/cutout-browser`, port18902, 21 real Chromium lifecycle checks passed including browser proof, two separate client payments, report decryption/key recovery, outsider rejection, revocation and new approval gate. Original services and public contracts untouched. The new test credential is now revoked by its test; do not present18902 as an enrolled fresh demo. Public bundle preview18903 serves `.runtime/review-pass-vercel` and its current read API; older18901 process has an old loaded API and is unsuitable for acceptance. Local UI test screenshots/evidence are in `.runtime/cutout-browser/evidence`. Read-only Cutout UI/walkthrough verifier: `node docs/design/cutout/verify-browser.mjs <base-url> <evidence-dir>`.

Prior public milestone now confirmed: snapshot `19ad2c00d93447d69c66d153bbe804c686303cd8d83fd448dc58b26eb49efaf7` matches finalized root and is active; all three Fuji contracts source-verified. Rehearsal client has20testUSDC and0.1TiramisuGLM, both rehearsal browser keys version1 registered. Hosted prover evidence is committed under `docs/review-pass/evidence/hosted-prover`. The funded public review and Arkiv write/native expiry have not run. Client/reviewer Swarm sessions in isolated19222/19223 browsers were pending user login when branding took priority. Do not ask again for wallet funds, Swarm IDs or recovery phrases. Preserve their profiles and registered keys. Public deployment remains at the existing Review Pass Vercel project; record the new Cutout deployment below after promotion. Do not deploy root EXIT Vercel configuration.

---

# Current restart handoff — Review Pass judge pass

Updated 12 September 2026. Root `/Users/razvan/Repos/real-eth-rome`, branch **`review-pass/product`**, preserved `main`. User asks for an implemented judge-readiness pass and agent/team checklist; explicitly ignore their earlier ScopeLift footer remark. Normal commits/pushes and bounded isolated subagents are authorized. Do not restart research/EXIT or change product direction.


## Latest: contracts deployed; publication and rehearsal funding require participant action

User explicitly authorized autonomous public rollout. Clean source **a9d0181** deployed all three contracts on Fuji with finalized success receipts: escrow `0xb431e570d506168711cc1f9f91e325b3114c62af`, verifier `0x9c1293e7e499d56fdc52186e7624dcfa04dfcbd1`, keys `0x181db4e48a0e76fdc50101085ac3b831464792c1`. All three have exact runtime source matches on Sourcify. Public-only manifest/receipts: [rollout evidence](review-pass/evidence/fuji-rollout/README.md). Do not rerun deployment. The deployer is the explicitly trusted test issuer/root authority and arbitrator; client/reviewer are separate.

The local manifest `.runtime/review-pass-fuji/deployment.json` has `snapshotPublication: pending` and no snapshot.reference. Contracts are real and directly callable; hosted financial UI remains disabled. Public whole snapshot is170bytes, SHA256 `0xb6f119bc297631f7b7af8f5ae39fb9bbf569efb9b15ab14751ec77995e52d156`, root4192859339826665750369528433721474123040425665956367961767766058523807596338. Exact vetted public issuer/snapshot files are in `experiments/qualification/pilot/hosting/publication/` and published as static metadata, not yet verified on Swarm. Root's private registry is `.runtime/review-pass-fuji/issuer`; never read/log/share private issuer material.

Current HTTPS deployment `dpl_AmeSTCQExKT7jF6T6fafJiRvYf5f` at the same Review Pass URL adds **Publish demo issuer snapshot** below the storage test. It uploads ONLY the build-reviewed original public bytes, independently retrieves/verifies, and supports retrieval retry without reupload. No custom filepicker/private fields. Fresh Chromium metadata-hash, capability gate, iframe,390px and0pageexceptions checks passed. The user must refresh/connect and click this button in their authenticated Swarm ID browser, then provide its reference. Agent browser has no such session; only FUJI_PRIVATE_KEY is configured. Do not extract browser secrets or reuse the user's postage signer with uncoordinated batch indices.

After the actual snapshot reference arrives:
`node experiments/qualification/pilot/publish-snapshot.mjs <reference>`
then `node experiments/qualification/pilot/hosting/build.mjs --config .runtime/review-pass-fuji/deployment.json` and the existing prebuilt Vercel deploy command. Finalizer and active build were actually tested to reject the connection-note reference/missing publication while preserving the manifest. The finalizer validates exact byte hash and current finalized issuer/root. Do not substitute a connection test for the snapshot or publish private holder/credential files.

Two fresh operator-controlled rehearsal wallets were generated in protected `.runtime/review-pass-fuji/rehearsal/wallets.json`, keys never logged. Public addresses in addresses.json: client `0x7259c94d93d83E539B57ba7A360cB06Bd9b53a60`, reviewer `0x7292C1652e831031175947daEcC7F85bF86381A1`. Each received0.05testAVAX from authorized deployer, finalized receipts in gas-funding.json. Both currently have0USDC/0GLM. User's original funded client/reviewer wallets are untouched. Required transfer to the new rehearsal **client**:10canonical Fuji testUSDC and0.03Tiramisu testGLM. No additional AVAX or key export needed. Wallets.json shape is `{client:<privateKey>, reviewer:<privateKey>}`; if the Arkiv runner needs a role-specific file, create an ignored0600`{privateKey:...}` file locally without printing it.

Operator test holder/credential are privately prepared at `.runtime/review-pass-fuji/rehearsal/{holder,credential}.json`, class7,172800-second initial expiry; inspect time before use. These are operator-controlled demonstration files, not an independently accredited user. Browser WASM actually generated a proof using this credential and the public setup; deployed Fuji verifier accepted a synthetic UNFUNDED context via eth_call, altered recipient rejected. [Crypto evidence/replay](review-pass/evidence/fuji-crypto-probe/README.md). No proof-acceptance transaction or paid job exists yet.

New `arkiv-public-probe.mjs`/`.md` reuses application listing/board policy for two independent software WSS clients and natural expiry. Root syntax check and actual read-only missing-job guard passed; worker tested six offline guards and two actual empty-filter WSS clients. Funded publication remains unrun; browser UI/reconnect/irrelevant-event evidence also remains outstanding. Do not claim software observers are physical browser users. Next implement/run funded public lifecycle with the configured role wallets; actual Swarm report uploads still need an authorized usable session, not fixtures. Existing local services were preserved.

The older dated entries below preserve earlier rollout states; the latest section above governs current deployment/access status.

## Public Swarm connection verified; retrieval endpoint corrected

The user imported sponsor postage into their existing Swarm ID account (reported 7.1 GB, estimated expiry September 16) and uploaded a generated public note through the hosted app. Independent retrieval initially failed because our configured `https://gateway.ethswarm.org/bytes/...` returns the gateway website HTML with HTTP200. The corrected Bee API origin is `https://api.gateway.ethswarm.org`. Defaults, setup page, Fuji deployer, public manifest builder and CSP now use that origin; no digest check was relaxed.

Production deployment `dpl_3HkvAAqBsRkyydWRXc2r3kfW4dKq` is live at the same Review Pass URL. Real application-adapter download and fresh production-origin Chromium both retrieved the user's142-byte note and matched SHA-256.19storage/read-API tests passed,1optional network test skipped; TypeScript/build and hosted browser checks passed. [Evidence](review-pass/evidence/swarm-public/README.md). This clears the usable-postage prerequisite, not the public encrypted-report/Fuji lifecycle. No supplied postage signer or recovery phrase was saved, deployed or committed. Next: public issuer metadata/whole snapshot and Fuji deployment, then two-participant rehearsal.

## Vercel follow-up and newly supplied access

Application revision **69de065** adds an isolated public-only Vercel build and browser bootstrap;64db620 adds the reviewed read-only serverless API. Actual site **https://review-pass-ethrome-2026.vercel.app** is deployed and fresh-browser verified without Vercel login. It is explicitly **pending public contracts**, with real Swarm ID initialization and an upload-credit-gated public test note. No financial actions, sample tasks or fake live flows are exposed. [Hosting runbook](../experiments/qualification/pilot/hosting/README.md), [deployment evidence](review-pass/evidence/vercel/README.md).

Vercel project review-pass-ethrome-2026 under mihrazvans-projects; CLI login configured. Build only `.runtime/review-pass-vercel/.vercel/output`, deploy from `.runtime/review-pass-vercel` with --prebuilt --prod. Root vercel.json still belongs to EXIT. Vercel linking auto-connected Git; root explicitly disconnected that new project's Git integration to avoid root EXIT auto-builds. Link command can reconnect it again; use the existing linked stage. The new project needs **no secret environment variables**. Both Vite builds explicitly disable env-file loading and implicit public-directory copying after independent review. Inspect build.json for the22published-file hashes.

User-funded public wallets are now verified: client0x92AAe0857979a139344f5b6F008e71F27A507522 (0.5FujiAVAX/20testUSDC), deployer0xB9080458DE79F614DB5d5208AB31bbF22A33DeAd (~4FujiAVAX), reviewer0x3a4205C246a48eC073ED4B3D12951cd4203713dA (0.5FujiAVAX). Client also received0.1TiramisuGLM, successful faucet tx0xa8ad9fb30f2e03e56c22c2e58933376221a5310d2e06093850a5f37a91578921. Private .env now contains FUJI_PRIVATE_KEY; derived address matched deployer without logging the key. Do not print/load it into Vercel. Public role addresses and local access status are under ignored `.runtime/review-pass-fuji/participants.json` and access-status.json.

User's Swarm account identity0xF8C832983A346cFc2D163C3cb488b1e011A13735 and data-sharing address0xa775D743B5f81665f76eB1575ac860eCb13b70F6 are public account information, not upload capability. The user has now imported sponsor postage and completed the public connection-note upload (verified above). **Do not ask for or store their Swarm recovery phrase.** They can connect Swarm ID on the new HTTPS page and, after credit activates, click the public test upload. User said to handle access one step at a time. No outward messages were sent. Public issuer metadata/current snapshot/arbitrator selection remain unset; funded deployment access is no longer the blocker.

Both GitHub CI workflows passed at69de065 (qualification34696465188, EXIT34696465179). Final follow-up is docs/evidence only. Verification:9read-API tests passed; pending build rejects local manifest; hosted public HTTP returns HTTP 200home/config,503snapshot,405POST,404privatepaths. Actual Chromium bothroles, Swarm iframe,390px and0pageexceptions pass. Active public promotion is prepared but unexecuted until the manifest/snapshot exist. Existing local18889/Anvil/Bee services remain untouched; auxiliary publicbundle preview18901 runs from `.runtime/review-pass-vercel/serve.mjs` (safe to stop its specific process). See subsequent ledger for prior complete local20-check lifecycle.

Start with **[shared judge checklist](review-pass/JUDGE-READINESS.md)**, [latest evidence](review-pass/evidence/judge-pass/README.md), [runbook](review-pass/RUNBOOK.md), [qualification provisioning](review-pass/QUALIFICATION-PROVISIONING.md), then [research](review-pass/README.md). Public targets remain Team1 Avalanche Track A, Arkiv Mission02/Mission03/Best Use and Swarm. Canonical Fuji test USDC meets the stablecoin-product requirement; custom issuance is not required. Current official Arkiv guidance conflicts with older materials; do not invent sponsor sign-off or completed submissions.

Implemented this pass: role-specific journeys; live wallet/gas/reward/device-key/storage readiness; network help; collapsed advanced controls; exact deadline-aware actions; mobile/focus improvements; clear report download feedback. Real browser-local WASM proving now reads issued credential + holder files locally, downloads pinned public parameters, reconstructs the whole snapshot, generates the existing Groth16 relation, simulates actual escrow acceptance and then offers a separate wallet signature. Cancellation/account/network/disconnect/page exit stop the worker and stale results cannot be cached. Ten-minute/task/credential expiry bound, bounded streams and sanitized private-input errors are included. No server signer/prover/private-file API was introduced.

Key commits: `46b1347` judge UX, `b4c82b5` guarded local task seed, `980c172` browser feasibility, **`5ad9366` integrated browser prover**, `7847025` provisioning guide, **`dea5ba2`** missing-route path sanitization and explicit local-task discovery link. Later commits add evidence/docs; inspect Git for exact HEAD. Qualification and EXIT CI both passed at the final application revision dea5ba2; run IDs and scope are archived in judge-pass/ci.json. Final handoff changes only docs/evidence.

Verification at5ad9366: **20 real Chromium lifecycle checks passed**, zero page exceptions, four independent browser profiles using public Anvil wallet bridges. Browser-generated proof settled against the actual deployed local verifier; two clients received isolated encrypted reports and approved both payments. Cancellation, wallet races, revocation/stale snapshot, outsiders, reload, key rotation, downloads and390px layout passed.18contract tests including256fuzz cases;57application tests pass/1optional network case skipped; Go tests, explicit pilot TypeScript, upload-security and preflight self-tests pass. Independent reviewers challenged prover integration and fresh judge entry. dea5ba2 received targeted actual HTTP/browser checks: five absent/private paths return generic404, snapshot200, correct manifest/verifier, open task preserved and390px no overflow.

**Current manual app: http://127.0.0.1:18889**. Config `.runtime/review-pass-judge/config.json`, owned PID `.runtime/review-pass-judge/pids.json` (inspect current PID and command before stopping). Escrow `0x1613beb3b2c4f22ee086b2b38c1476a3ce7f78e8`, key registry `0xa82ff9afd8f496c3d6ac40e2a0f282e47488cfc9`. Fresh credential/current snapshot; real **open task1 funded with250local qUSD**, seed repeat verified REUSED with0writes. Source5ad9366 lifecycle used a different escrow recorded in browser.json. Do not consume this manual task during a read-only audit.

Private issuer/holder material is under `.runtime/review-pass-judge`; `holder/latest.json` contains local file paths for supervised selection, never an HTTP download. Device keys still require each wallet's browser registration. The full browser test deliberately revokes its credential and leaves a stale pointer. To prepare another manual rehearsal afterward, use the runbook's fresh local deployment, restart only18889 and seed; never undo revocation or reset issuer/chain history. Seed history prevents blind replacement; inspect its journal before deliberate --new.

Shared Anvil31338 on18547 and Bee1633/1635 remain running. **Do not reset the original qualification chain.** Earlier18888/18787 and EXIT5173/8547/8787 services are preserved. The local qUSD/test issuer are not public bounty integrations or actual professional accreditation.

Public `.runtime/review-pass-fuji` remains **PREPARED_NOT_DEPLOYED**, with separate experimental setup and pinned source/setup/circuit hashes. `--broadcast` requires an authorized funded project signer, matching public issuer metadata, actual whole snapshot on public Swarm and intended arbitrator. Preserve partial journals/locks and inspect receipts before recovery. Public server startup builds WASM using Go and requires setupDir/setupHashes; only explicit public files are served.

Latest read-only preflight is archived in judge-pass/public-preflight.json: Fuji/Arkiv RPCs and Swarm health reachable; no configured project signers, public deployment or HTTPS app. The preflight's missing token/upload-endpoint fields are not claims that canonical USDC is absent or Swarm ID cannot upload: public USDC exists, and browser Swarm ID needs an actual usable session/postage. No public writes, faucet claims, outward messages or form submissions occurred.

Remaining agent work depends on those concrete resources: deploy public contracts, execute canonical-USDC lifecycle; real Arkiv funded creation/native expiry and two-client WSS recording; public Swarm upload/independent retrieval/recipient decryption; host and verify final HTTPS URL; prepare final receipt-linked bounty submission evidence. Team provides project access/test wallets/postage, issuer/reviewer counterpart and independent-device participation, obtains final submission links/video and submits forms. See T01–T11 for ownership. Do not call the project public-release ready before those gates.

Limits: test issuer, single-process Groth16 setup, no standards interoperability/adoption, manual first enrollment, Chromium desktop proving only, wallet reuse links jobs, key recovery/retention and arbitrator trust. Recipient-key rotation while a wallet transaction is pending is not atomically prevented. Focused independent reviews are not a production audit.

All bounded workers completed; root integrated attributed commits and owns pushes. Keep their worktrees/history. One older Arkiv worker's uncommitted terms.ts copy is its test dependency, not root dirtiness; leave it alone.

---

## Earlier pilot handoff — preserved history

# Restart handoff — Review Pass qualification experiment, EXIT preserved

Updated 2026-09-12 Europe/Bucharest. Workspace `/Users/razvan/Repos/real-eth-rome`, origin `MihRazvan/eth-rome-2026`, branch `main`. Nearby `/Users/razvan/Repos/eth-rome-2026` is a different active project; leave it untouched. Preserve supplied handoff files and actual Git provenance.

## Latest user objective — implement the wallet-separated pilot

The user said “Lets do it” after the Review Pass feasibility result. Implementation is pushed through `9fb24a9`; the final evidence/handoff commits add documentation. Read the [pilot overview](../experiments/qualification/pilot/README.md), [runbook](../experiments/qualification/pilot/RUNBOOK.md), [acceptance](../experiments/qualification/pilot/ACCEPTANCE.md) and [independent review](../experiments/qualification/pilot/SECURITY.md) first. Preserve EXIT, REPRISE and the original qualification workbench.

Implemented: injected-wallet transaction ownership with no server signer/prover endpoint; holder-local public proof import; onchain wallet-owned P-256 key registry; browser IndexedDB private-key persistence/history/rotation; recipient-encrypted document delivery authenticated by the assigned worker's onchain locator+digest; durable issuer allocation/permanent revocation with concurrent-process/SIGKILL tests. This is the local issuer-led rehearsal, not real issuer/client adoption or a completed public deployment.

Latest actual integration: **13 real Chromium checks** with one credential, reviewer, two client profiles and an outsider; two distinct job-scoped nullifiers; client-specific encryption; both clients pay after durable credential revocation/root publication; reload, rotation, disconnect and stale wallet-connect regression. It uses explicit public Anvil EOA test bridges, not installed wallet extensions/independent physical devices.17 contract tests,10 key tests, full Go suite, upload signature probes, preflight self-test, TypeScript and Vite build passed. Public evidence and receipts are under `experiments/qualification/pilot/evidence`; no private credential/issuer/holder files were copied.

New pilot: **http://127.0.0.1:18888**, PID in `.runtime/qualification-pilot/pids.json`. It reuses the original runtime's Anvil31338/18547, test token and prover setup/verifier. Do not restart the original runtime casually: that resets the pilot's chain. The pilot server rebuilds UI and loads config at startup. Latest recorded test escrow is `0xb7f8bc63bbcad18155201308c8f3540b07f84f5e`; read the live config for current addresses. The browser test leaves two paid jobs, the credential revoked and the configured snapshot pointer stale. For a fresh rehearsal, run `node experiments/qualification/pilot/deploy-local.mjs`, then restart only18888. This consumes a new durable credential slot and deploys new contracts; never reset/unrevoke the issuer to rerun a demo.

Private local runtime files: issuer under `.runtime/qualification-pilot/issuer`, holder under `.runtime/qualification-pilot/holder`, private latest-file pointer `holder/latest.json`; public config `config.json`; runnable prover `prover`; setup `.runtime/qualification/setup`. Keep private data out of Git/HTTP. The deployment script controls local test roles on one host. For a real pilot the issuer only receives the holder commitment and the holder proves on their own machine.

Fresh public preflight08:12UTC on September12: Fuji43113 block58330456, Arkiv7738577 block346200 and Swarm public health reachable. No project `.env`/configured funded Fuji or Arkiv signer/public contracts/public Swarm upload authority. No public writes or outreach. Arkiv adapters exist but assignment WSS/discovery is not integrated into this pilot UI. Public submission evidence and actual issuer/two-client adoption remain open. An optional async question about known assessment/client teams is unanswered; do not treat silence as recruitment or block independent work.

Limits: EOA-only upload authorization; localhost hosting/in-memory quotas; current onchain key availability can block delivery; no old-key cross-device recovery; snapshot pointer must be updated when root changes; issuer whole-disk rollback/malicious-keyholder protection is absent; experimental Groth16 setup and trusted arbitrator remain. The fixed250test-qUSD/class7 UI is a rehearsal, not a general assessment marketplace. Read [participant worksheet](../experiments/qualification/pilot/PILOT.md) before a real test; outreach draft is unsent.

All bounded workers completed and their attributed changes were integrated. Existing EXIT services5173/8547/8787, original workbench18787/18547, local Bee1633/1635 and unrelated8545Anvil/Kurtosis remain untouched. Meaningful next steps: configured public deployment/write access, public Arkiv discovery/native-expiry reconciliation, a hosted service policy, real wallet-extension/device testing and a willing issuer/two clients. Do not relabel more local rehearsals as those results.

## Earlier qualification feasibility milestone — preserved history

The user selected the private-qualification direction for sustained implementation/probing, with Arkiv/Swarm/Avalanche prioritized and ENS deprioritized. This supersedes REPRISE as the active experiment. Read [qualification README](../experiments/qualification/README.md), [acceptance](../experiments/qualification/ACCEPTANCE.md), [runbook](../experiments/qualification/RUNBOOK.md) and [product verdict](../experiments/qualification/research/product.md). The implemented concept is **REVIEW PASS**, portable qualification for confidential paid technical reviews. Cryptographic feasibility is positive; commercial demand and public sponsor completion are not established.

Implementation is integrated/pushed through `0f46801`: independently constructed gnark BN254/Groth16 signature+secret+same-index nonrevocation circuit, actual exported Solidity verifier, escrow with approval/timeout/dispute rules, issuer-wide local Bee snapshots, browser AES-GCM artifact storage/retrieval, Arkiv status/assignment adapters and a working local three-role UI. This is **not a reproduction of ShadowPath**, credential-standard interoperability, anonymous employment or an anonymous payment system. Research/source inventory and two rendered concepts are retained. EXIT and REPRISE remain intact below.

Runtime follow-up `bb65172` selects its fresh verifier artifact by compiler provenance, avoiding a basename collision with fixed proof fixtures. Its fresh23-check integration passed and is archived separately as `evidence/runtime-followup*`. The current browser helper was left freshly seeded and ready at the URL below. Final evidence/handoff commits are documentation-only; inspect Git for the exact remote head rather than assuming0f46801 is still HEAD.

Evidence:23 real-proof/Anvil/Bee integration checks;6 browser checks;14 Solidity tests (four actual-proof fixtures and ten explicit state-machine tests,256 dispute fuzz inputs);13 transport tests/typecheck; independent11 crypto boundary probes. Independent review found/corrected class-width mismatch and helper concurrency. Qualification CI [34681651818](https://github.com/MihRazvan/eth-rome-2026/actions/runs/34681651818) and existing EXIT CI [34681651824](https://github.com/MihRazvan/eth-rome-2026/actions/runs/34681651824) both passed at0f46801. Local evidence records runtime source7b69991 and is archived under `experiments/qualification/evidence/` with hashes; later CI/tests do not alter runtime behavior.

Local workbench: **http://127.0.0.1:18787**, dedicated Anvil31338/18547. Inspect `.runtime/qualification/pids.json` for current owned processes. Fresh startup automatically runs its23checks, rolls back only those local test transactions, then seeds a new250qUSD assignment. Browser tests intentionally consume the seeded job; restart the experiment for a fresh manual demo, confirming the PID before TERM. All role keys/files are local TEST data; do not expose this helper as a hosted prover. Only browser ciphertext reaches Bee, but cross-device key delivery/recovery is not implemented and a closed tab loses its key.

Six experiment-owned `bee-factory-*` containers remain on loopback: queen1633, independent retrieval1635, factory Anvil28545. Current local mock postage is recorded in `transport/local-stack.json`; it expires and can be repurchased locally per runbook. Do not stop unrelated Docker/Kurtosis containers or the pre-existing8545Anvil. EXIT5173/8547/8787 remain unrelated.

Public gates freshly observed September12: Arkiv reads/SDK WebSocket notifications and Fuji RPC work; **no funded project Arkiv/Fuji signer or usable public Swarm write capability**. Public publication/native expiry/Fuji deployment remain blocked. Assignment-specific WSS UI reconciliation, public two-client role separation and recipient key delivery remain unfinished; no bounty completion claim. No external outreach, sponsor submission or real funds spent. Candidate pilot needs one willing assessment collective and two real clients; none secured.

Operational issuer limit: CLI signs caller-supplied indices. Production needs atomic unique slot allocation, monotonic tombstones, fresh index on reissue and key/registry rotation. Slot reuse or same-key registry reset can revive unexpired old credentials. The demo Restore action is intentionally reversible test behavior. Depth16 under nonreuse supports65,536 lifetime issuances per issuer key.10,000revocations produced108KB snapshot and~795ms local reconstruction; no browser-scale claim.

Next useful work once access exists: preserve holder-local witness custody, add real wallet-separated roles/key delivery, deploy the verified path to Fuji, publish actual Arkiv task/snapshot records and observe native expiry/WSS, store/retrieve on public Swarm, then record real issuer/client pilot evidence. Do not restart broad ideation or return to EXIT merely because live access is absent. Agents completed bounded work in isolated worktrees; their attributed commits are integrated.

## Earlier hard-pivot recommendation — REPRISE retained as research

The latest user explicitly requested a **hard pivot to maximize hackathon prize prospects**, any product category, informed by July–September research and the team's prior winners ANYWARE (`ETH_Prague_Hackathon`) and AUTARK. This replaces the conditional withdrawal-NFT direction below. Read [pivot recommendation](pivot/README.md), [sponsor rules/probes](pivot/sponsors.md), [frontier screen](pivot/frontier.md) and [prior-winner analysis](pivot/winners.md) first. Existing EXIT requirements/evidence remain historical; do not silently present EXIT as the new product.

The integrator's lead recommendation is **REPRISE**: a narrowly supported paid job recoverable by another owner-authorized operator without the original provider/database or treasury key. This is a **research recommendation and isolated feasibility prototype**, not a launched application or validated business. BATON was an early working name, discarded after finding an adjacent existing Agent Baton Protocol. Restate, DBOS, Handover and other prior art are explicitly acknowledged. The proposed distinction is cross-operator recovery with portable encrypted inputs and independent payment authority, not invention of retries, idempotency or signed context.

`docs/pivot/recovery-probe/` is a self-contained Foundry project. Its final worker run and independent integrator rerun passed **10 tests including 256 fuzz cases**, using local exact-transfer test tokens and simulated EVM time. It does not use RPC/Anvil or kill a real process. It checks immutable owner-approved steps, payment consumption, worker leases/epochs, stale-worker rejection and cancellation/refunds. Opaque receipt hashes do not prove external evidence truth. The owner can still approve the same business invoice under distinct step IDs. See [integration evidence](pivot/integration-evidence.json) for exact scope.

The decisive next technical gate is a **cold second operator** with no primary database, hidden shared files or treasury key: retrieve encrypted Swarm inputs, reconcile the first included payment after lost acknowledgement, obtain fresh Fuji authority, finish remaining supported work, and reject the revived stale worker. Original inputs must be available before payment. Key envelopes must preauthorize a chosen backup; arbitrary future workers do not automatically decrypt. Operator fee/gas, cancellation, retained plaintext and unsupported external side effects need explicit treatment. No full replacement product was deployed in the research round.

Current sponsor corrections: Arkiv hub lists EUR awards, at most **EUR 1,000 per team**, with revised scoring; the main ETHRome prize is in-kind annual Urbe Hub memberships. Arkiv + Swarm + Team1-A has a conditional maximum **EUR 1,000 + USD 900**, plus an uncertain ENS share only if genuinely qualified. ENSv2 authority on Sepolia is separate from Fuji financial authority. Public Arkiv raw WSS and SDK subscriptions returned actual events; nine current ENSv2 contracts had bytecode and consistent read relationships; Fuji responded. These are read-only successes, not write/bounty completion. Existing access gates below remain last observed, not a freshly rerun preflight.

Alternatives retained: SWITCH issuer-recognized multiparty reservations (strong consumer demo, significant prior art and issuer/density gate); private temporary-work qualification (recent ShadowPath mechanism, actual prover/issuer path unrun); ALL IN group consent (clear but crowded); Mandate scoped procurement (four-sponsor fit, incremental novelty). No external customer outreach, real-money action or sponsor submission occurred. Three research workers used isolated worktrees from `920d66d`; their attributed commits and the recovery probe are integrated. No active worker has unfinished ownership.

## Earlier EXIT viability decision — superseded product direction

The user questioned viability, then explicitly authorized deep research and changing direction to find a viable product. Read [viability assessment](viability/README.md) and [decision 002](decisions/002-viability-pivot.md) before adding features. The evidence rejects the assumption that retail BENQI/liquid-token exits are the best launch market. The conditional candidate is an **embedded service for already queued, noncancellable Lido unstETH or standard ether.fi NFTs on Ethereum**. This is not validated demand, a source partnership or permission to spend mainnet funds.

Three independent worktrees researched Avalanche exit alternatives, native claim rights/observed queues, and demand/distribution. Primary-source inventories, executable probes, exact blocks and raw observations are in `docs/viability/`. Origin ARM is a serious incumbent for liquid-token exits; Intentional is closer existing-NFT prior art. Product novelty and the addressable market must not be exaggerated.

At Avalanche 95044397, observed 1–100 sAVAX exits lost approximately 1.362 bps against contemporaneous NAV; existing BENQI EOA requests cannot import and cancellation returns shares. Bad large single-route quotes do not establish best-execution slippage. At Ethereum 25956536, Lido had 290 pending NFTs/20,789.2103 stETH face/241 owner addresses; standard ether.fi had 29 valid pending/2,240.3261 ETH face/17 owners. These are concentrated public inventories, not willing customers. A reconciled 10,000-block log sample is only 1.3943 days; an apparently successful wider RPC response was truncated and is not treated as seven-day evidence.

The next commercial evidence is actual holder situations, independent funded bids, an unsubsidized acceptable price and practical distribution. [PILOT.md](viability/PILOT.md) prepares questions and an unfilled worksheet. No external outreach, customer acceptance or maker commitment occurred. A prior optional question asking whether the user knows an actual queued holder/protocol contact remains unanswered; do not block independent work or infer interest from silence. Do not send unsolicited messages to third parties.

## Delivered and verified

**Exit Check:** `/?check=1` reads a real Ethereum NFT without a wallet or the trading API. It reports source/owner/state/implementation at one block. Finalized means source-reported amount, not simulated collectibility; permission, escrow and callback conditions can still prevent collection. Deleted ether.fi records are closed, not proof of a payout. Implementation drift disables the buyer calculator. RPC failures never substitute fixtures. Pending face and elapsed age are not guaranteed proceeds or a remaining ETA.

The optional calculator requires explicit recovery, wait, funding hurdle, loss haircut, costs, reserve and fee assumptions. Integer ETH/WETH amounts and conservative rounding are used. It never invents a buyer or seller floor. Purchase costs explicitly exclude the separately entered reserve. No native-NFT settlement is enabled. Both candidate sources collect/burn whole NFTs; test-vault partial payments are not source functionality.

Final presentation implementation **945e626a31561b1c141f9f5c7bde625eaa078339** is pushed. Full `npm run check` at 0d747c7 passed TypeScript, **71 JS tests**, **39 reported Solidity tests** (16 inherited repetitions) and build. Two invariants ran 128×64 calls each with zero reverts. Full browser suite passed **15 tests in 1.6m**: ten actual local trading/privacy/navigation tests and five explicitly mocked-RPC checker boundary tests. The final one-line reserve-label correction passed typecheck/build and all five affected checker browser tests. ABI regeneration left no changes. The existing optional Node-crypto externalization and large-bundle warnings remain; actual browser crypto works.

`npm run test:source -- --no-storage-caching` at 6f8bf90 passed **7/7**: three BENQI tests at 95031281 plus four native Ethereum tests at 25956536. Native probes use dRPC by default (`EXIT_NATIVE_FORK_RPC` override); Publicnode started rejecting historical reads as archive requests, while current reads still worked. The native tests assert exact errors, preserve pending request economics through transfer/resale, and collect existing finalized Lido #135117 and ether.fi #82510 to the new owner with full burn and repeat-claim rejection. No native source time/balance/rate/operator overrides. BENQI's existing redemption test explicitly simulates future operator publication; do not mix those scopes. No public transaction or sale adapter was exercised.

Independent review found and corrected finalized/claimable ambiguity, inactive legacy fee labeling, and reserve-cost labeling. It independently checked 2,211 fee-rounding cases and 48 funding-budget scenarios. A fresh independent 390×844 browser read at 25956664 had no errors/overflow. See [review](viability/economics-review.md), [source evidence](viability/claim-sources.md), and [integrated evidence](evidence/viability-integration.json).

Remote CI for final presentation implementation: [34646434601](https://github.com/MihRazvan/eth-rome-2026/actions/runs/34646434601). It **passed** clean install, type/tests/build, ABI consistency and all 15 Chromium scenarios. Full metadata is in `docs/evidence/viability/ci.json`. Pinned-source CI is dispatch-only and is not implied by a push pass. Evidence-only commits after the implementation do not change the checked application code.

## Existing product boundaries to preserve

The separate local product still implements settlement, backed test-source origination, exact public/private offers, HPKE P-256 device keys, onchain key registry, maker/client/runtime, collection, resale and recovery. Enabled market collateral is the disclosed test vault. BENQI remains a separate admission prototype.

Prior research fixed exact-private-bid ERC20 approval leakage and cross-wallet asynchronous plaintext overwrite. Private funding uses a separately disclosed 100,000 test-USDC allowance independent of price, with reuse. Refreshes bind to session and onchain request context. Discovery outage preserves owner collection; key certificates retry publication; incomplete HTTP bodies cannot block the signer queue. Navigation/dialog focus and confidential input cleanup are tested. Preserve these boundaries; read [earlier research](research/README.md) and use the repository `$exit-verify` skill for future releases.

## Services, hosting and access

The integrator-owned local stack remains at **http://127.0.0.1:5173**: Anvil 8547, API 8787, Vite 5173. Owned child PIDs are in `.runtime/pids.json`, launcher output `.runtime/research/ready-stack.log`. Browser verification has created and traded local test claims; it is not a freshly reset seed. Coordinate before restarting an active user demo. The checker alone can run with `npm run dev` when these ports are free; it does not need Anvil/API.

Hosted **https://exit-ethrome-2026.vercel.app/?check=1** is a real read-only Ethereum inspector. `?preview=1` remains explicitly labelled visual fixtures; the default trading route has no hosted backend. Vercel deployment **dpl_H1mBcgRXFtvYLvkzbqeok5pSKKUh** uses final presentation implementation 945e626 (deployment checkout 32b23fb adds review documentation only). Actual hosted browser evidence is in `docs/evidence/browser-viability/`; hosting metadata is in `docs/evidence/hosting.json`.

Latest read-only preflight at 20:52 UTC remained blocked: no project `.env`, four distinct Fuji deployer/maker keys, Arkiv signer, Fuji manifest, Swarm upload endpoint or funded postage. Fuji 43113 and Arkiv 7738577 answered; Arkiv discovery reads and Swarm gateway health passed. See [preflight](evidence/viability/preflight.json). No Fuji lifecycle, Arkiv write/native expiry or Swarm uploaded-byte round-trip was claimed.

If resources arrive, the existing [deployment guide](DEPLOYMENT.md) and [human test guide](USER-TEST.md) cover the authorized test-vault end-to-end test. An Ethereum-native product does not satisfy Avalanche settlement merely by reading Ethereum through a Fuji UI. Keep that bounty tradeoff explicit. Human submission/conversation/attendance/camera steps remain outstanding in `docs/SUBMISSION.md`.

Research agents `design`, `review` and `transport` finished isolated assignments and their commits were integrated without rewriting provenance. No active worker needs continuation. Current gaps are commercial evidence and public integration resources, not a missing generic dashboard or more self-trades.
