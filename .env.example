# Deaddrop's local walkthrough needs no secrets. Never expose keys via VITE_ variables.
# For the full public deployment see docs/deaddrop/DEPLOYMENT.md.
FUJI_RPC_URL=https://api.avax-test.network/ext/bc/C/rpc
ARKIV_RPC_URL=https://rpc.tiramisu.db-chain.testnet.arkiv.network
# Optional server-only encrypted enrollment relay gas key. The deployed service
# retains this compatibility name; it is NOT the issuer's credential-signing key.
CUTOUT_ENROLLMENT_RELAY_KEY=
